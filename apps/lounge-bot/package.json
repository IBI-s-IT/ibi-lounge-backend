{
  "name": "lounge-bot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NO_CACHE=1 NODE_PATH=. ts-node-dev --transpile-only --require dotenv/config ./src/main.ts | pino-pretty",
    "start": "cross-env NODE_PATH=dist node dist/index.js",
    "build": "tsup-node",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist && rm *.tsbuildinfo",
    "test": "jest --setupFiles dotenv/config",
    "lint": "eslint . --quiet",
    "lint-fix": "eslint . --fix"
  },
  "dependencies": {
    "@repo/generators": "*",
    "@repo/api-schema": "*",
    "@repo/shared": "*",
    "@grammyjs/auto-retry": "^1.1.1",
    "@grammyjs/menu": "^1.2.1",
    "@grammyjs/runner": "^2.0.3",
    "@grammyjs/storage-redis": "^2.4.0",
    "@grammyjs/transformer-throttler": "^1.2.1",
    "grammy": "^1.19.1",
    "grammy-inline-menu": "^8.0.1",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "@types/ioredis": "^5.0.0",
    "cross-env": "^7.0.3",
    "eslint": "^9.9.1",
    "tsup": "8.3.0"
  }
}
